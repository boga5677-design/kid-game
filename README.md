# Kid Game v0.6.6 - Home icon circle

本版修正：

- 首頁 8 張遊戲卡左側圖示改為統一圓形底座
- 移除原本每張卡片左側不一致的白色殘塊感
- 圖示改為置中顯示，大小與留白一致
- 保留 v0.6.5 的其他功能與 build fix

建議重新 build：

```bash
./gradlew --no-daemon assembleDebug
```
